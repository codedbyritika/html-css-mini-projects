# Squares Hover Effect

Mini project using HTML and CSS.

## What I Learned
- Basic HTML structure
- Use of `<div>` and `id` for layout
- CSS Flexbox for centering elements
- Creating different shapes using CSS:
  - Square
  - Circle (using border-radius)
  - Diamond (using rotate)
- Applying hover effects using `:hover`

